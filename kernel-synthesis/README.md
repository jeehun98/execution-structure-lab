# kernel-synthesis

이 디렉토리는 단순한 prompt 기반 코드 생성이 아니라, 구조적 제약을 반영한 kernel generation 방식을 다룹니다.

생성 대상은 computation structure, preservation legality, primitive availability, composition rule, hardware-aware constraint에 의해 정의됩니다.

목표는 코드 생성을 자유 생성이 아니라 constrained realization으로 다루는 것입니다.