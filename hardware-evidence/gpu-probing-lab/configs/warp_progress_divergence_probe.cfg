# Warp Progress Divergence Probe
#
# 같은 block 안에 있는 4개 warp에 서로 다른 task를 부여하고,
# 같은 cycle_budget 동안 warp별 progress가 실제로 어떻게 갈라지는지 관찰한다.
#
# warp 0: fast_independent_alu
# warp 1: dependent_alu_chain
# warp 2: shared_load
# warp 3: dependent_global_load

blocks=1
cycle_budget=200000000
sample_period=256
global_elements=16777216